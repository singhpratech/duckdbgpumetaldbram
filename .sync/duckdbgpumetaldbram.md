# duckdbgpumetaldbram
- branch: main
- working on: v0.7 on main: exact GROUP BY (NULLs, HUGEINT, DECIMAL, DATE/TIMESTAMP, packed and VARCHAR keys, WHERE mask) rewritten through the Python wrapper on Metal; thresholds + SF1 gate; next: CUDA kernels (Linux), stddev/var + count(DISTINCT), OR in WHERE, date-arithmetic folding
- status: in_progress
- blocked on: nothing
- last update: 2026-09-17T04:38:10Z
